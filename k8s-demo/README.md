# goca + cert-manager demo

The full walkthrough for this directory's manifests has moved to the docs
site: **[docs/kubernetes/cert-manager.md](../docs/kubernetes/cert-manager.md)**
(or the hosted version, once published, linked from the repo's main
[README.md](../README.md)).

This directory still holds the runnable manifests the walkthrough references:
[`cluster-issuer.yaml`](cluster-issuer.yaml) and
[`certificate.yaml`](certificate.yaml). For the CSI driver demo (mounting
secrets directly, no cert-manager involved), see
[`csi/`](csi/README.md).
