# goca + Secrets Store CSI Driver demo

The full walkthrough for this directory's manifests has moved to the docs
site: **[docs/kubernetes/csi.md](../../docs/kubernetes/csi.md)** (or the
hosted version, once published, linked from the repo's main
[README.md](../../README.md)).

This directory still holds the runnable manifests the walkthrough
references: [`rbac.yaml`](rbac.yaml), [`csidriver.yaml`](csidriver.yaml),
[`provider-daemonset.yaml`](provider-daemonset.yaml),
[`secretproviderclass.yaml`](secretproviderclass.yaml), and
[`pod.yaml`](pod.yaml). For the cert-manager/ACME demo instead, see
[`../`](../README.md).
